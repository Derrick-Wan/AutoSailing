pub mod finding_mod{
    pub fn find_fish(period: i32) -> bool{

        // 如果没有发现鱼群
        return false;
    }
}

pub mod trace_fish{
    pub fn get_distance() -> i32{
        // 返回这次和上次的距离之差
        return 0
    }

    pub fn get_location() -> i32{
    //     返回角度
        return 30
    }
}

